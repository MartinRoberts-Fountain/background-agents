apiVersion: v1
kind: Service
metadata:
  name: {{ include "helm-deployer.fullname" . }}
  labels:
    {{- include "helm-deployer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "helm-deployer.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
