{{- if gt (int .Values.replicaCount) 1 }}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "helm-deployer.fullname" . }}
  labels:
    {{- include "helm-deployer.labels" . | nindent 4 }}
spec:
  minAvailable: 1
  selector:
    matchLabels:
      {{- include "helm-deployer.selectorLabels" . | nindent 6 }}
{{- end }}
